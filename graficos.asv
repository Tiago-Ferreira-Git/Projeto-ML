clear('all')
close('all')
to_plot = load('Projeto/to_plot_mse.mat');
figure;
plot(to_plot.lasso_vector,to_plot.mean_squared_error_x);
hold on;
xlabel('\lambda');
ylabel('Mean Squared Errors');
grid on;
hold off

for j=1:length(KI)